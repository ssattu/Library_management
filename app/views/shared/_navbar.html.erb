<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Library</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <%= link_to books_path,class:"nav-link" do %>
           <i class="fa fa-home"></i> Home
        <% end %>
        </li>

        <% if user_signed_in? && current_user.is_admin == false %>
          <li>
          <%= link_to 'Issue New Book', new_issue_book_path, class:"nav-link"%>
          </li>
          <li>
          <%= link_to 'View Issue Book',  issue_books_index_path, class:"nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to 'Return Issue Book',  issue_books_index_path, class:"nav-link"%>
          </li>
        <% end %>
      
      </ul>
      <%= form_with url: books_path,  method: :get, class:"d-flex" do |f| %>
        <%= f.text_field :name, class:"form-control me-2" %>
        <%= f.submit :Search,   class:"btn btn-outline-success m-1" %>
      <% end %>
    
      <ul class="navbar-nav mb-2 mb-lg-0">
          <% if user_signed_in? %>
              <% if user_signed_in? && current_user.is_admin %>
                <li class="nav-item">
                  <%= link_to 'Add Book', new_book_path, class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= link_to 'View Issue Book',  issue_books_index_path, class:"nav-link"%>
                </li>
              <% end %>

            <li class="nav-item">
              <%= link_to 'Sign Out', destroy_user_session_path, class:" btn-outline-danger rounded-3 nav-link"%>
            </li>
          <% else %>
          <li class="nav-item d-block m-1">
            <%= link_to new_user_session_path, class:"nav-link d-flex align-items-center btn btn-outline-dark rounded-3"  do %>
            <i class="fas fa-user-check"> </i> Sign_In 
            <% end %>
          </li>
          <li class="nav-item d-block m-1">
            <%= link_to new_user_registration_path, class:"nav-link d-flex align-items-center btn btn-outline-dark rounded-3" do %>
              <i class="fas fa-user"></i> Sign_up 
            <% end %>
          </li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
<div  class="position-absolute top-3">
    <%= link_to "Back", :back, class:"btn btn-secondary m-5" %>
  </div>